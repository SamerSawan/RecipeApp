class Ingredient {
    constructor (
        id,
        name,
        amount,
    ) {
        this.id = id;
        this.name = name;
        this.amount = amount;
    }
}

export default Ingredient;